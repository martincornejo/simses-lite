# Contributing

Contributions are welcome. The full guide — dev setup, test commands, PR conventions, style notes — lives in the documentation:

→ **[docs/contributing.md](docs/contributing.md)** (source)
→ **[Contributing page on the docs site](https://tum-ees.github.io/simses/contributing/)** (rendered)

If you just want the commands:

```bash
uv sync --all-groups
uv run pytest
uv run ruff check .
uv run ruff format .
```

See the linked guide for everything else.
